# Настройка запуска в Cursor Full Stack App

## 🚨 Проблема
При запуске через Cursor Full Stack App возникает ошибка:
```
ModuleNotFoundError: No module named 'fastapi'
```

## ✅ Решение

### Вариант 1: Использование скрипта запуска (Рекомендуется)
```bash
# В терминале Cursor выполните:
cd /home/urch/Projects/yandex_downloads
./run_backend.sh
```

### Вариант 2: Ручной запуск с активацией venv
```bash
# В терминале Cursor выполните:
cd /home/urch/Projects/yandex_downloads/backend
source venv/bin/activate
export PYTHONPATH="$PWD:$PYTHONPATH"
python main.py
```

### Вариант 3: Использование start.sh
```bash
# В терминале Cursor выполните:
cd /home/urch/Projects/yandex_downloads
./start.sh backend
```

## 🔧 Конфигурация для Cursor

### 1. Настройка Python интерпретатора
В Cursor:
1. Откройте Command Palette (Ctrl+Shift+P)
2. Выберите "Python: Select Interpreter"
3. Выберите: `/home/urch/Projects/yandex_downloads/backend/venv/bin/python`

### 2. Настройка launch.json
Создан файл `.vscode/launch.json` с правильными настройками для отладки.

### 3. Настройка .cursorrules
Создан файл `.cursorrules` с инструкциями для Cursor.

## 🚀 Полный запуск приложения

### Backend + Frontend одновременно:
```bash
cd /home/urch/Projects/yandex_downloads
./start.sh
```

### Только Backend:
```bash
cd /home/urch/Projects/yandex_downloads
./run_backend.sh
```

### Только Frontend:
```bash
cd /home/urch/Projects/yandex_downloads/frontend
npm run dev
```

## 🔍 Проверка работоспособности

### Проверка Backend:
```bash
curl http://localhost:8000/api/health
# Должен вернуть: {"status":"ok"}
```

### Проверка Frontend:
```bash
curl http://localhost:3000
# Должен вернуть HTML страницу
```

### Проверка токенов:
```bash
curl http://localhost:8000/api/tokens
# Должен вернуть список токенов
```

## 📋 Структура проекта

```
/home/urch/Projects/yandex_downloads/
├── backend/
│   ├── venv/                    # Виртуальное окружение Python
│   ├── main.py                  # Основной файл FastAPI
│   ├── db_manager.py           # Менеджер базы данных
│   └── requirements.txt        # Python зависимости
├── frontend/
│   ├── node_modules/           # Node.js зависимости
│   ├── src/                    # Исходный код React
│   └── package.json           # Node.js зависимости
├── .vscode/
│   └── launch.json            # Конфигурация для отладки
├── .cursorrules               # Правила для Cursor
├── run_backend.sh            # Скрипт запуска backend
└── start.sh                  # Основной скрипт запуска
```

## ⚠️ Важные замечания

1. **Всегда используйте виртуальное окружение** для backend
2. **Активируйте venv** перед запуском Python кода
3. **Установите правильный Python интерпретатор** в Cursor
4. **Используйте скрипты запуска** для избежания ошибок

## 🎯 Готовые команды для копирования

### Запуск всего приложения:
```bash
cd /home/urch/Projects/yandex_downloads && ./start.sh
```

### Запуск только backend:
```bash
cd /home/urch/Projects/yandex_downloads && ./run_backend.sh
```

### Проверка статуса:
```bash
curl http://localhost:8000/api/health && curl http://localhost:3000
```

## 🎉 Результат

После выполнения этих шагов:
- ✅ Backend запускается без ошибок
- ✅ Frontend работает корректно
- ✅ Все API эндпоинты доступны
- ✅ Токены сохраняются и работают
- ✅ Приложение готово к использованию
