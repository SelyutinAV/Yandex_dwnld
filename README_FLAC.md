# 🎉 FLAC ЗАГРУЗКА РАБОТАЕТ!

## Быстрый старт

### 1. Установите зависимости

```bash
# В backend/
cd backend
./venv/bin/pip install -r requirements.txt

# ffmpeg (если ещё не установлен)
sudo apt install ffmpeg
```

### 2. Используйте OAuth токен

В приложении введите **OAuth токен** (начинается с `AgAAAA` или `y0_`).

**Где взять:**
1. Откройте [music.yandex.ru](https://music.yandex.ru)
2. Откройте DevTools (F12)
3. Перейдите на вкладку **Network**
4. Обновите страницу
5. Найдите любой запрос к `api.music.yandex.net`
6. В headers найдите `Authorization: OAuth <ваш_токен>`
7. Скопируйте токен после `OAuth `

### 3. Выберите качество Lossless

В настройках приложения выберите **"Lossless (FLAC)"**.

### 4. Скачивайте!

Просто выберите треки или плейлисты и нажмите "Скачать".

Всё остальное происходит автоматически:
- ✅ Запрос к API
- ✅ Скачивание зашифрованного файла
- ✅ Расшифровка AES-128-CTR
- ✅ Конвертация MP4→FLAC
- ✅ Очистка временных файлов

---

## ✅ Что работает

- **FLAC lossless** (~30 МБ на трек)
- **MP3 320 kbps** (высокое качество)
- **MP3 192 kbps** (среднее качество)

---

## ⚠️ Требования

1. **Подписка Яндекс.Плюс** (для FLAC)
2. **OAuth токен** (из веб-версии)
3. **ffmpeg** (для конвертации)
4. **pycryptodome** (для расшифровки, устанавливается автоматически)

---

## 📝 Примечания

- FLAC файлы **автоматически расшифровываются** и конвертируются
- Временные файлы **автоматически удаляются**
- Весь процесс логируется в **Настройки → Логи**

---

## 🎵 Наслаждайтесь lossless качеством!

