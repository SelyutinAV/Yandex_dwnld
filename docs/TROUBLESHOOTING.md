# 🔧 Устранение проблем при запуске

## Найденные проблемы и решения

### ❌ Проблема 1: "Команда «python» не найдена"

**Ошибка:**
```
Команда «python» не найдена, но может быть установлена с помощью:
sudo apt install python-is-python3
```

**Решение:**
```bash
# В Linux используйте python3
cd backend
python3 -m venv venv
source venv/bin/activate

# Теперь внутри виртуального окружения 'python' работает
python main.py
```

### ❌ Проблема 2: "npm не найден"

**Ошибка при запуске frontend:**
```
npm command not found
```

**Решение:**
```bash
# Установка Node.js и npm
sudo apt update
sudo apt install nodejs npm

# Проверка
node --version
npm --version
```

### ❌ Проблема 3: Зависимости не установлены

**Симптомы:**
- ModuleNotFoundError при запуске Python
- Ошибки импорта

**Решение:**
```bash
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

cd ../frontend
npm install
```

### ❌ Проблема 4: Порты заняты

**Ошибка:**
```
ERROR: [Errno 98] Address already in use
```

**Решение:**
```bash
# Найти процессы на портах 8000 и 3000
sudo lsof -i :8000
sudo lsof -i :3000

# Убить процессы
sudo kill -9 PID_NUMBER

# Или перезагрузить систему
```

## ✅ Проверка работоспособности

### Backend тест:
```bash
cd backend
source venv/bin/activate
python main.py

# В другом терминале:
curl http://localhost:8000/api/health
```

Ожидаемый ответ:
```json
{"status": "ok"}
```

### Frontend тест:
```bash
cd frontend
npm run dev
```

Откройте: http://localhost:3000

## 🚀 Правильная последовательность запуска

### 1. Проверка системы
```bash
# Проверить Python
python3 --version

# Проверить Node.js (если нужно установить)
sudo apt install nodejs npm

# Проверить npm
npm --version
```

### 2. Установка зависимостей
```bash
# Backend
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Frontend  
cd ../frontend
npm install
```

### 3. Создание .env файла
```bash
cd backend
cp env.example .env
# Отредактируйте .env и добавьте токен
nano .env
```

### 4. Запуск через Cursor

**Теперь используйте Cursor:**
- `Ctrl+Shift+P` → "Tasks: Run Task"
- Выберите "🚀 Start Full Stack"

Или горячие клавиши:
- `Ctrl+Shift+B` - запуск всего

## 🎯 Альтернативные команды запуска

Если задачи Cursor не работают:

```bash
# Терминал 1 - Backend
cd backend
source venv/bin/activate
python main.py

# Терминал 2 - Frontend  
cd frontend
npm run dev
```

## 📋 Диагностические команды

### Проверка портов:
```bash
netstat -tlnp | grep :8000  # Backend порт
netstat -tlnp | grep :3000  # Frontend порт
```

### Проверка процессов:
```bash
ps aux | grep python  # Backend процессы
ps aux | grep node    # Frontend процессы
```

### Тест API:
```bash
curl -X GET http://localhost:8000/api/health
curl -X GET http://localhost:8000/docs  # Swagger UI
```

## 🔍 Логи и отладка

### Backend логи:
- Запуск: "Uvicorn running on http://0.0.0.0:8000"
- Ошибки: Red текст в терминале

### Frontend логи:
- Запуск: "Local: http://localhost:3000"
- Ошибки: Browser DevTools или терминал

### Общие проблемы:
1. **Файл .env не найден** → Создайте из env.example
2. **Токен недействителен** → Получите новый токен Яндекс.Музыки
3. **CORS ошибки** → Проверьте, что backend на порту 8000
4. **Модули не найдены** → Переустановите зависимости

## 🆘 Если ничего не помогает

1. **Чистая переустановка:**
```bash
# Удалить виртуальные окружения
rm -rf backend/venv
rm -rf frontend/node_modules

# Переустановить все
cd backend && python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt
cd ../frontend && npm install
```

2. **Проверить версии:**
```bash
python3 --version  # Должно быть 3.10+
node --version     # Должно быть 18+
npm --version      # Любая современная
```

3. **Перезагрузка системы** и повторить установку

---

**Главное правило:** Сначала установите все зависимости, затем запускайте приложение! 🎯
