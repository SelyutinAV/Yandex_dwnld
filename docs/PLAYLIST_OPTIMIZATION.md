# Оптимизация обработки крупных плейлистов

## Обзор

Реализована оптимизированная система обработки крупных плейлистов, таких как "Мне нравится" с 7971 треком. Система использует пакетную обработку (batch processing) для эффективной работы с большими объемами данных.

## Основные изменения

### Backend

#### 1. `yandex_client.py`

**Новые методы:**

- `get_playlist_tracks(playlist_id, batch_size=100, max_tracks=None)` - обновленный метод с поддержкой батчей
- `_get_liked_tracks_optimized(batch_size, max_tracks)` - специальная оптимизированная обработка плейлиста "Мне нравится"
- `_process_tracks_batch(tracks, batch_size)` - универсальный метод пакетной обработки треков

**Ключевые особенности:**

- **Batch Processing**: Треки обрабатываются батчами по 100 штук (настраивается)
- **Rate Limiting**: Задержка 0.5 сек между батчами для защиты от блокировки API
- **Эффективное использование API**: Используется метод `client.tracks(batch_ids)` для получения информации о множестве треков за один запрос вместо множества вызовов `fetch_track()`
- **Ограничение количества треков**: Возможность обработать только первые N треков для тестирования

#### 2. `db_manager.py`

**Новые методы:**

- `get_playlist_settings()` - получение настроек обработки плейлистов
- `update_playlist_settings(settings)` - обновление настроек

**Структура настроек:**

```python
{
    'batch_size': 100,           # Размер батча
    'max_tracks': None,          # Ограничение треков (None = все)
    'enable_rate_limiting': True # Включить задержки между батчами
}
```

#### 3. `main.py`

**Новые эндпоинты:**

- `GET /api/settings/playlist` - получение настроек плейлистов
- `POST /api/settings/playlist` - сохранение настроек плейлистов

**Обновленные эндпоинты:**

- `GET /api/playlists/{playlist_id}/tracks` - теперь использует настройки для оптимизации

### Frontend

#### 1. `SettingsPanel.tsx`

**Новая секция:** "Плейлисты"

**Настройки:**

1. **Размер батча** (10-500 треков)
   - Рекомендуется: 100
   - Большие значения ускоряют обработку, но могут привести к таймаутам

2. **Ограничение треков** (опционально)
   - Позволяет обработать только первые N треков
   - Полезно для тестирования на больших плейлистах

3. **Rate Limiting** (вкл/выкл)
   - Добавляет задержку между запросами
   - Рекомендуется включать для избежания блокировки

**UI элементы:**

- Интуитивные подсказки с описанием параметров
- Примеры рекомендуемых настроек для разных сценариев
- Информационные блоки с объяснениями

## Производительность

### До оптимизации

- **Проблема**: Плейлист "Мне нравится" (7971 трек) вызывал зависание приложения
- **Причина**: Синхронные вызовы `fetch_track()` для каждого трека (7971 API-запрос)
- **Время обработки**: ~40-60 минут или таймаут

### После оптимизации

- **Решение**: Пакетная обработка по 100 треков за раз
- **API-запросов**: ~80 вместо 7971
- **Время обработки**: ~2-3 минуты для полного плейлиста
- **Стабильность**: Нет зависаний, контроль над процессом

## Примеры использования

### 1. Полная обработка плейлиста "Мне нравится"

```
Батч: 100
Лимит: (пусто)
Rate Limiting: включен

Результат: ~80 запросов, ~2-3 минуты
```

### 2. Быстрое тестирование

```
Батч: 50
Лимит: 100
Rate Limiting: включен

Результат: ~2 запроса, ~1-2 секунды
```

### 3. Максимальная скорость (рискованно)

```
Батч: 200
Лимит: (пусто)
Rate Limiting: выключен

Результат: ~40 запросов, ~1-1.5 минуты
⚠️ Риск блокировки API
```

## Рекомендации

### Для больших плейлистов (1000+ треков)

1. ✅ Включите Rate Limiting
2. ✅ Используйте батч 100-150
3. ✅ Первый раз протестируйте с лимитом 500 треков
4. ✅ Следите за логами для отслеживания прогресса

### Для средних плейлистов (100-1000 треков)

1. ✅ Rate Limiting можно выключить
2. ✅ Батч 100-200
3. ✅ Лимит не требуется

### Для малых плейлистов (<100 треков)

1. ➖ Оптимизация не критична
2. ➖ Любые настройки подойдут

## Логирование

Система выводит подробные логи процесса обработки:

```
🔄 Получаем плейлист 'Мне нравится' (оптимизированно)...
✅ Получено 7971 лайков
📋 Получено 7971 ID треков
📦 Обрабатываем батч 1/80 (100 треков)
✅ Батч 1/80 обработан: 100 треков
📦 Обрабатываем батч 2/80 (100 треков)
...
✅ Успешно обработано 7971 треков из 'Мне нравится'
```

## Технические детали

### Алгоритм обработки "Мне нравится"

1. Получение списка лайкнутых треков: `client.users_likes_tracks()`
2. Извлечение ID треков из результата
3. Разделение ID на батчи (по умолчанию 100)
4. Для каждого батча:
   - Вызов `client.tracks(batch_ids)` для получения полной информации
   - Обработка и форматирование данных треков
   - Задержка 0.5 сек (если Rate Limiting включен)
5. Возврат объединенного списка

### Преимущества подхода

- **Масштабируемость**: Работает с плейлистами любого размера
- **Надежность**: Защита от таймаутов и блокировок API
- **Гибкость**: Настраиваемые параметры для разных сценариев
- **Прозрачность**: Подробное логирование процесса
- **Контроль**: Возможность ограничения для тестирования

## Troubleshooting

### Проблема: Таймауты при обработке

**Решение:**
- Уменьшите размер батча до 50
- Включите Rate Limiting
- Попробуйте с ограничением 500-1000 треков

### Проблема: Блокировка API

**Решение:**
- Включите Rate Limiting
- Уменьшите размер батча
- Подождите 10-15 минут перед повторной попыткой

### Проблема: Медленная обработка

**Решение:**
- Увеличьте размер батча до 150-200
- Выключите Rate Limiting (на свой риск)
- Проверьте скорость интернет-соединения

## Будущие улучшения

- [ ] Асинхронная обработка с `asyncio`
- [ ] Прогресс-бар в реальном времени в UI
- [ ] Возможность остановки/паузы обработки
- [ ] Кэширование обработанных треков
- [ ] Инкрементальная синхронизация (только новые треки)

