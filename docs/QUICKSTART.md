# Быстрый старт

Это краткое руководство для быстрого запуска приложения.

## Предварительные требования

✅ Node.js 18+ и npm установлены  
✅ Python 3.10+ установлен  
✅ Есть аккаунт Яндекс.Музыки с подпиской  

## Установка за 5 минут

### 1. Установка зависимостей Backend

```bash
cd "/home/urch/ Projects/yandex_downloads/backend"
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 2. Установка зависимостей Frontend

```bash
cd "../frontend"
npm install
```

### 3. Настройка

Создайте файл `/home/urch/ Projects/yandex_downloads/backend/.env`:

```env
YANDEX_TOKEN=ваш_токен_яндекс_музыки
DOWNLOAD_PATH=/home/urch/Music/Yandex
DEFAULT_QUALITY=lossless
DATABASE_URL=sqlite+aiosqlite:///./yandex_music.db
API_HOST=0.0.0.0
API_PORT=8000
DEBUG=True
```

**Как получить токен:**
1. Откройте https://music.yandex.ru в браузере
2. Откройте DevTools (F12) → Network
3. Авторизуйтесь на сайте
4. Найдите любой запрос к API в Network
5. Скопируйте значение заголовка `Authorization`

### 4. Запуск

**Терминал 1 - Backend:**
```bash
cd "/home/urch/ Projects/yandex_downloads/backend"
source venv/bin/activate
python main.py
```

**Терминал 2 - Frontend:**
```bash
cd "/home/urch/ Projects/yandex_downloads/frontend"
npm run dev
```

### 5. Открыть приложение

Откройте браузер: **http://localhost:3000**

## Первые шаги

1. **Настройки** → Введите токен → Проверить
2. **Плейлисты** → Обновить → Выбрать плейлисты → Синхронизировать
3. **Загрузки** → Следить за прогрессом
4. **Файлы** → Просмотр статистики

## Рекомендации для аудиофила

🎵 **Качество:** Всегда выбирайте "Максимальное" (FLAC 24-bit)  
💿 **Хранение:** Используйте SSD для лучшей скорости  
📁 **Организация:** Файлы автоматически сортируются по артистам/альбомам  
🔄 **Синхронизация:** Настройте автосинхронизацию раз в день  

## Возможные проблемы

**npm не найден:**
```bash
sudo apt install npm
```

**Python версия < 3.10:**
```bash
sudo apt install python3.10 python3.10-venv
```

**Ошибка при установке yandex-music:**
```bash
pip install --upgrade pip setuptools wheel
pip install -r requirements.txt
```

## Полная документация

- [SETUP.md](SETUP.md) - Подробное руководство по установке
- [ARCHITECTURE.md](ARCHITECTURE.md) - Архитектура приложения
- [../README.md](../README.md) - Общая информация

---

**Наслаждайтесь музыкой в максимальном качестве! 🎧**

