# ⚡ СУПЕР БЫСТРЫЙ СТАРТ в Cursor

## 🚀 За 30 секунд в Cursor:

### 1. Установка зависимостей
```bash
Ctrl+Shift+P → Tasks: Run Task → 📦 Install All Dependencies
```

### 2. Создание .env файла
```bash
Ctrl+Shift+P → Tasks: Run Task → setup-env
```
Затем отредактируйте `backend/.env` - добавьте токен Яндекс.Музыки

### 3. Запуск приложения
```
Ctrl+Shift+B
```

**Всё!** 🎉

---

## 🎯 Что происходит:

- ✅ Backend запустится на http://localhost:3333
- ✅ Frontend откроется на http://localhost:7777  
- ✅ Два терминала откроются автоматически
- ✅ Логи видны в панели TERMINAL

---

## 🎵 Что дальше:

1. Откройте http://localhost:7777 в браузере
2. Перейдите в "Настройки"
3. Введите токен Яндекс.Музыки
4. Нажмите "Проверить"
5. Перейдите в "Плейлисты"
6. Нажмите "Обновить" → выберите плейлисты → "Синхронизировать"

---

## 📋 Настройка Cursor IDE

### 🎯 Tasks (Задачи)
- `🚀 Start Full Stack` - запуск всего проекта (основная задача)
- `🐍 Backend: Run Server` - только backend сервер
- `⚛️ Frontend: Run Dev Server` - только frontend приложение
- `🧪 Test Backend API` - тестирование API
- `📦 Install All Dependencies` - установка всех зависимостей

### ⌨️ Горячие клавиши
- `Ctrl+Shift+B` - **запустить полный стек**
- `Ctrl+Shift+R` - запустить только backend
- `Ctrl+Shift+F` - запустить только frontend  
- `Ctrl+Shift+T` - тест backend API

### 🔧 Debug конфигурации
- `🚀 Backend Server` - обычный запуск backend
- `🐍 Backend Debug` - debug режим с точками останова

### 🛠️ Первоначальная настройка

1. **Установка зависимостей через Cursor**:
   - `Ctrl+Shift+P` → `Tasks: Run Task` → `📦 Install All Dependencies`

2. **Создание .env файла**:
   - `Ctrl+Shift+P` → `Tasks: Run Task` → `setup-env`
   - Затем отредактируйте `backend/.env` - добавьте токен Яндекс.Музыки

### 🔍 Отладка

**Python Backend**:
1. Установите точку останова в `.py` файле (клик на номер строки)
2. `F5` → выберите `🐍 Backend Debug`
3. Операции отладки: F10 (step over), F11 (step into), F5 (continue)

**TypeScript Frontend**:
1. Установите точку останова в `.tsx` файле
2. Chrome DevTools также доступны

### 🚨 Устранение проблем

**Backend не запускается**:
1. Проверьте, что виртуальное окружение создано
2. Убедитесь, что `.env` файл существует
3. Запустите `🔧 Backend: Install Dependencies`

**Frontend не запускается**:
1. Проверьте, что `node_modules` установлены
2. Запустите `🎨 Frontend: Install Dependencies`

**Порты заняты**:
Если 3333 или 7777 порты заняты, остановите процессы:
```bash
pkill -f "python.*main.py"
pkill -f "vite"
```

---

## ⌨️ Полезные горячие клавиши:

- `Ctrl+Shift+B` - запуск всего проекта
- `Ctrl+Shift+T` - тест API
- `F5` - отладка backend
- `Ctrl+C` в терминалах - остановка

---

**Наслаждайтесь музыкой! 🎧**
