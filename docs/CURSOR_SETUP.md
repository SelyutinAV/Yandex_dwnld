# 🚀 Настройка запуска из Cursor IDE

Проект полностью настроен для удобной работы в **Cursor IDE**!

## 📋 Что настроено

### 🎯 Tasks (Задачи)
- `🚀 Start Full Stack` - запуск всего проекта (основная задача)
- `🐍 Backend: Run Server` - только backend сервер
- `⚛️ Frontend: Run Dev Server` - только frontend приложение
- `🧪 Test Backend API` - тестирование API
- `📦 Install All Dependencies` - установка всех зависимостей

### ⌨️ Горячие клавиши
- `Ctrl+Shift+B` - **запустить полный стек**
- `Ctrl+Shift+R` - запустить только backend
- `Ctrl+Shift+F` - запустить только frontend  
- `Ctrl+Shift+T` - тест backend API

### 🔧 Debug конфигурации
- `🚀 Backend Server` - обычный запуск backend
- `🐍 Backend Debug` - debug режим с точками останова

## 🚀 Как запустить

### Способ 1: Command Palette (рекомендуется)

1. Нажмите `Ctrl+Shift+P` (Command Palette)
2. Введите: `Tasks: Run Task`
3. Выберите: **`🚀 Start Full Stack`**

### Способ 2: Горячие клавиши

Просто нажмите `Ctrl+Shift+B` и всё запустится!

### Способ 3: Панель задач

1. `Ctrl+Shift+P` → `Tasks: Run Task`
2. Выберите нужную задачу

## 📊 Мониторинг

### Терминалы
- Backend запустится в отдельном терминале (порт 8000)
- Frontend в другом терминале (порт 3000)
- Все логи будут видны в "TERMINAL" панели

### Отладка
1. `F5` или `Run and Debug` панель
2. Выберите конфигурацию `🚀 Backend Server`
3. Установите точки останова в Python коде

## 🛠️ Первоначальная настройка

### 1. Установка расширений
Cursor автоматически предложит установить рекомендуемые расширения:
- Python
- TypeScript и JavaScript
- ESLint
- Prettier

### 2. Установка зависимостей через Cursor

1. `Ctrl+Shift+P` → `Tasks: Run Task`
2. Выберите: **`📦 Install All Dependencies`**

Или по отдельности:
- `🔧 Backend: Install Dependencies`
- `🎨 Frontend: Install Dependencies`

### 3. Создание .env файла

1. `Ctrl+Shift+P` → `Tasks: Run Task`
2. Выберите: **`setup-env`** (создаст .env из примера)

Или вручную:
```bash
cp backend/env.example backend/.env
# Затем отредактируйте backend/.env
```

## 🎯 Полезные команды

### Проверка здоровья
```bash
Ctrl+Shift+T  # Проверить, что backend отвечает
```

### Открытие в браузере
- Frontend: http://localhost:3000
- Backend API: http://localhost:8000/docs

### Перезапуск
Просто остановите задачи (`Ctrl+C` в терминалах) и запустите заново через `Ctrl+Shift+B`

## 🔍 Отладка

### Python Backend
1. Установите точку останова в `.py` файле (клик на номер строки)
2. `F5` → выберите `🐍 Backend Debug`
3. Операции отладки: F10 (step over), F11 (step into), F5 (continue)

### TypeScript Frontend
1. Установите точку останова в `.tsx` файле
2. `F5` → выберите конфигурацию frontend
3. Chrome DevTools также доступны

## 🎨 Автодополнение и IntelliSense

### Python
- Автоимпорты включены
- Типизация с подсказками
- Автоформатирование при сохранении

### TypeScript/React
- Автокомплит компонентов
- Проверка типов в реальном времени
- Автоимпорты React компонентов

## 📁 Структура файлов в Cursor

Проект настроен для удобной навигации:
- Группировка связанных файлов
- Скрытие `node_modules`, `venv`, `__pycache__`
- Быстрый поиск только по исходному коду

## 🚨 Устранение проблем

### Backend не запускается
1. Проверьте, что виртуальное окружение создано
2. Убедитесь, что `.env` файл существует
3. Запустите `🔧 Backend: Install Dependencies`

### Frontend не запускается
1. Проверьте, что `node_modules` установлены
2. Запустите `🎨 Frontend: Install Dependencies`
3. Убедитесь, что npm установлен

### Порты заняты
Если 8000 или 3000 порты заняты, остановите процессы:
```bash
pkill -f "python main.py"
pkill -f "vite"
```

## 🎵 После запуска

1. **Backend**: Откроется http://localhost:8000/docs (API документация)
2. **Frontend**: Откроется http://localhost:3000 (веб-приложение)
3. В настройках frontend введите токен Яндекс.Музыки
4. Начните синхронизацию плейлистов!

---

**Теперь разработка стала еще удобнее! 🎉**

Просто нажмите `Ctrl+Shift+B` и наслаждайтесь музыкой в максимальном качестве! 🎧
