# Логика сортировки качества аудио

## 🎯 Исправленная сортировка качества

Теперь система правильно сортирует качество аудио с учетом **битовой глубины** и **частоты дискретизации**, а не только общего уровня качества.

## 📊 Порядок сортировки (от лучшего к худшему)

### 1. **Lossless качество** (безпотерьное)

| Порядок | Качество            | Описание                     |
| ------- | ------------------- | ---------------------------- |
| 1       | **24-bit/48.0kHz**  | Лучшее lossless качество     |
| 2       | **24-bit/44.1kHz**  | Хорошее lossless качество    |
| 3       | **16-bit/48.0kHz**  | Стандартное lossless высокое |
| 4       | **16-bit/44.1kHz**  | Стандартное lossless         |
| 5       | **Другое lossless** | Остальные lossless форматы   |

### 2. **Сжатое качество** (lossy)

| Порядок | Качество    | Описание                     |
| ------- | ----------- | ---------------------------- |
| 6       | **320kbps** | Высокое сжатое качество      |
| 7       | **256kbps** | Хорошее сжатое качество      |
| 8       | **192kbps** | Среднее сжатое качество      |
| 9       | **128kbps** | Низкое сжатое качество       |
| 10      | **96kbps**  | Очень низкое сжатое качество |

### 3. **Неизвестное качество**

| Порядок | Качество    | Описание             |
| ------- | ----------- | -------------------- |
| 11      | **Unknown** | Неизвестное качество |

## 🔍 Логика определения качества

### Парсинг параметров:

1. **Битовая глубина**:

   - `24-bit` → 24
   - `16-bit` → 16
   - `32-bit` → 32

2. **Частота дискретизации**:

   - `48.0kHz` или `48kHz` → 48000 Hz
   - `44.1kHz` или `44kHz` → 44100 Hz
   - `32kHz` → 32000 Hz
   - `22kHz` → 22000 Hz

3. **Битрейт**:
   - `320kbps` или `320` → 320 kbps
   - `256kbps` или `256` → 256 kbps
   - `192kbps` или `192` → 192 kbps
   - `160kbps` или `160` → 160 kbps
   - `128kbps` или `128` → 128 kbps
   - `96kbps` или `96` → 96 kbps

## 🎵 Примеры правильной сортировки

### До исправления (неправильно):

1. 16-bit/44.1kHz
2. 24-bit/48.0kHz ❌

### После исправления (правильно):

1. **24-bit/48.0kHz** ✅ (лучше)
2. **16-bit/44.1kHz** ✅ (хуже)

## 🚀 Преимущества новой логики

1. **Точная сортировка**: Учитывает конкретные параметры качества
2. **Логичный порядок**: 24-bit/48kHz действительно лучше 16-bit/44.1kHz
3. **Расширяемость**: Легко добавлять новые форматы качества
4. **Консистентность**: Одинаковая логика в backend и frontend

## 🔧 Техническая реализация

### Backend (`audio_quality_utils.py`):

```python
def get_quality_sort_order(quality_string: str) -> int:
    # Парсинг битовой глубины, частоты дискретизации и битрейта
    # Возвращает число для сортировки (меньше = лучше)
```

### Frontend (`FileAnalyzer.tsx`):

```typescript
const getQualitySortOrder = (quality: string) => {
  // Аналогичная логика парсинга и сортировки
  // Возвращает число для сортировки (меньше = лучше)
};
```

## ✅ Результат

Теперь система качества аудио:

- ✅ Правильно сортирует 24-bit/48kHz выше 16-bit/44.1kHz
- ✅ Учитывает все параметры качества при сортировке
- ✅ Обеспечивает логичный порядок от лучшего к худшему
- ✅ Работает консистентно в backend и frontend
