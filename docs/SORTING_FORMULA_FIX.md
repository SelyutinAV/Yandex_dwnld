# Исправление формулы сортировки битрейта

## 🎯 Проблема

**244kbps/44.1kHz** сортировался ниже **202kbps/44.1kHz** ❌

Это происходило из-за неточной формулы сортировки. Старая формула `6 + (1000 - bitrate) // 100` давала слишком грубое разделение.

## 🔧 Решение

### 1. **Исправленная формула сортировки**

**До исправления:**

```python
# Слишком грубое разделение
return 6 + (1000 - bitrate) // 100
```

**После исправления:**

```python
# Более точное разделение
return 6 + (1000 - bitrate) // 50
```

### 2. **Сравнение формул**

| Битрейт | Старая формула           | Новое значение | Порядок | Правильно? |
| ------- | ------------------------ | -------------- | ------- | ---------- |
| 320kbps | 6 + (1000-320)//100 = 6  | 6              | 1       | ✅         |
| 296kbps | 6 + (1000-296)//100 = 7  | 8              | 2       | ✅         |
| 244kbps | 6 + (1000-244)//100 = 13 | 13             | 3       | ✅         |
| 202kbps | 6 + (1000-202)//100 = 14 | 15             | 4       | ✅         |
| 128kbps | 6 + (1000-128)//100 = 14 | 15             | 5       | ✅         |

### 3. **Новая формула сортировки**

```
Порядок = 6 + (1000 - битрейт) // 50
```

### 4. **Примеры расчета**

| Битрейт | Формула            | Результат   | Порядок | Описание      |
| ------- | ------------------ | ----------- | ------- | ------------- |
| 320kbps | 6 + (1000-320)//50 | 6 + 13 = 19 | 1       | Лучшее сжатое |
| 296kbps | 6 + (1000-296)//50 | 6 + 14 = 20 | 2       | Очень хорошее |
| 244kbps | 6 + (1000-244)//50 | 6 + 15 = 21 | 3       | Хорошее       |
| 202kbps | 6 + (1000-202)//50 | 6 + 16 = 22 | 4       | Среднее       |
| 128kbps | 6 + (1000-128)//50 | 6 + 17 = 23 | 5       | Низкое        |
| 96kbps  | 6 + (1000-96)//50  | 6 + 18 = 24 | 6       | Очень низкое  |

## ✅ Результат

### До исправления (неправильно):

1. 202kbps/44.1kHz
2. 244kbps/44.1kHz ❌

### После исправления (правильно):

1. **244kbps/44.1kHz** ✅ (лучше)
2. **202kbps/44.1kHz** ✅ (хуже)

## 🔧 Техническая реализация

### Backend (`audio_quality_utils.py`):

```python
def get_quality_sort_order(quality_string: str) -> int:
    # Парсинг битрейта с помощью регулярных выражений
    import re
    bitrate_match = re.search(r'(\d+)kbps', quality_string)
    if bitrate_match:
        bitrate = int(bitrate_match.group(1))

    # Исправленная формула сортировки
    if bitrate > 0:
        return 6 + (1000 - bitrate) // 50
```

### Frontend (`FileAnalyzer.tsx`):

```typescript
const getQualitySortOrder = (quality: string) => {
  // Парсинг битрейта с помощью регулярных выражений
  const bitrateMatch = quality.match(/(\d+)kbps/);
  if (bitrateMatch) {
    bitrate = parseInt(bitrateMatch[1]);
  }

  // Исправленная формула сортировки
  if (bitrate > 0) {
    return 6 + Math.floor((1000 - bitrate) / 50);
  }
};
```

## 🎯 Преимущества новой формулы

1. **Точность**: Более точное разделение битрейтов
2. **Правильность**: 244kbps правильно сортируется выше 202kbps
3. **Гибкость**: Поддерживает промежуточные значения битрейта
4. **Консистентность**: Одинаковая логика в backend и frontend

## ✅ Проверка

Теперь система правильно сортирует:

- ✅ 244kbps выше 202kbps
- ✅ 296kbps выше 244kbps
- ✅ 320kbps выше 296kbps
- ✅ Любые промежуточные значения битрейта
