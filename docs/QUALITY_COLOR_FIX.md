# Исправление логики определения цвета качества

## 🎯 Проблема

**128kbps/48kHz** и **128kbps/44.1kHz** имели одинаковый цвет ❌

Это происходило потому, что логика определения цвета учитывала только битрейт, но не частоту дискретизации. Например, 128kbps/48kHz должно быть лучше чем 128kbps/44.1kHz и иметь другой цвет.

## 🔧 Решение

### 1. **Комбинированный показатель качества**

**До исправления:**

```python
# Только битрейт
if '128kbps' in quality_string:
    return 'bg-gradient-to-r from-amber-500 to-orange-600'
```

**После исправления:**

```python
# Битрейт + частота дискретизации
quality_score = bitrate + (sample_rate / 1000)
if quality_score >= 170:  # 128kbps/48kHz или 160kbps/44.1kHz
    return 'bg-gradient-to-r from-amber-500 to-orange-600'
```

### 2. **Формула качества**

```
Качество = Битрейт + (Частота дискретизации / 1000)
```

### 3. **Примеры расчета**

| Качество        | Битрейт | Частота | Формула    | Результат | Цвет      |
| --------------- | ------- | ------- | ---------- | --------- | --------- |
| 128kbps/48kHz   | 128     | 48000   | 128 + 48   | 176       | Янтарный  |
| 128kbps/44.1kHz | 128     | 44100   | 128 + 44.1 | 172.1     | Оранжевый |
| 160kbps/44.1kHz | 160     | 44100   | 160 + 44.1 | 204.1     | Небесный  |
| 192kbps/48kHz   | 192     | 48000   | 192 + 48   | 240       | Синий     |

## 📊 Новая цветовая схема

### Диапазоны качества:

| Диапазон | Цвет                            | Примеры                         |
| -------- | ------------------------------- | ------------------------------- |
| ≥ 350    | `from-emerald-500 to-green-600` | 320kbps/48kHz, 320kbps/44.1kHz  |
| ≥ 300    | `from-teal-500 to-cyan-600`     | 256kbps/48kHz, 320kbps/44.1kHz  |
| ≥ 250    | `from-blue-500 to-blue-600`     | 192kbps/48kHz, 256kbps/44.1kHz  |
| ≥ 200    | `from-sky-500 to-blue-500`      | 160kbps/48kHz, 192kbps/44.1kHz  |
| ≥ 170    | `from-amber-500 to-orange-600`  | 128kbps/48kHz, 160kbps/44.1kHz  |
| < 170    | `from-orange-500 to-red-600`    | 128kbps/44.1kHz, 96kbps/44.1kHz |

## ✅ Результат

### До исправления (неправильно):

- 128kbps/48kHz = Янтарный
- 128kbps/44.1kHz = Янтарный ❌ (одинаковый цвет)

### После исправления (правильно):

- 128kbps/48kHz = Янтарный ✅ (лучше)
- 128kbps/44.1kHz = Оранжевый ✅ (хуже, другой цвет)

## 🔧 Техническая реализация

### Backend (`audio_quality_utils.py`):

```python
def get_quality_badge_color(quality_string: str) -> str:
    # Парсинг битрейта и частоты дискретизации
    bitrate_match = re.search(r'(\d+)kbps', quality_string)
    if bitrate_match:
        bitrate = int(bitrate_match.group(1))

    # Комбинированный показатель качества
    quality_score = bitrate + (sample_rate / 1000)

    # Определение цвета на основе комбинированного показателя
    if quality_score >= 350:
        return 'bg-gradient-to-r from-emerald-500 to-green-600'
    # ... и так далее
```

### Frontend (`FileAnalyzer.tsx`):

```typescript
const getQualityBadgeColor = (quality: string) => {
  // Парсинг битрейта и частоты дискретизации
  const bitrateMatch = quality.match(/(\d+)kbps/);
  if (bitrateMatch) {
    bitrate = parseInt(bitrateMatch[1]);
  }

  // Комбинированный показатель качества
  const qualityScore = bitrate + sampleRate / 1000;

  // Определение цвета на основе комбинированного показателя
  if (qualityScore >= 350) {
    return "bg-gradient-to-r from-emerald-500 to-green-600";
  }
  // ... и так далее
};
```

## 🎯 Преимущества новой логики

1. **Точность**: Учитывает и битрейт, и частоту дискретизации
2. **Дифференциация**: Разные цвета для разных комбинаций параметров
3. **Логичность**: 128kbps/48kHz действительно лучше 128kbps/44.1kHz
4. **Расширяемость**: Легко добавлять новые диапазоны качества

## ✅ Проверка

Теперь система правильно определяет цвет:

- ✅ 128kbps/48kHz имеет янтарный цвет (лучше)
- ✅ 128kbps/44.1kHz имеет оранжевый цвет (хуже)
- ✅ 160kbps/44.1kHz имеет небесный цвет (лучше чем 128kbps/48kHz)
- ✅ 192kbps/48kHz имеет синий цвет (лучше чем 160kbps/44.1kHz)
