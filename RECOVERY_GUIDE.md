# 🚨 Руководство по восстановлению приложения

## ✅ Проблема решена!

Файл `backend/main.py` был поврежден и восстановлен. Теперь приложение снова работает!

## 🚀 Как запустить приложение

### Вариант 1: Через терминал Cursor
```bash
cd /home/urch/Projects/yandex_downloads
./run_backend.sh
```

### Вариант 2: Ручной запуск
```bash
cd /home/urch/Projects/yandex_downloads/backend
source venv/bin/activate
export PYTHONPATH="$PWD:$PYTHONPATH"
python main.py
```

### Вариант 3: Полный запуск (Backend + Frontend)
```bash
cd /home/urch/Projects/yandex_downloads
./start.sh
```

## 🔧 Созданные скрипты

### `run_backend.sh` - Запуск только backend
- ✅ Останавливает процессы на порту 8000
- ✅ Активирует виртуальное окружение
- ✅ Устанавливает PYTHONPATH
- ✅ Запускает FastAPI сервер

### `run_frontend.sh` - Запуск только frontend
- ✅ Останавливает процессы на порту 3000
- ✅ Проверяет node_modules
- ✅ Запускает Vite сервер

## 📊 Проверка работоспособности

```bash
# Проверка backend
curl http://localhost:8000/api/health
# {"status":"ok"} ✅

# Проверка frontend
curl http://localhost:3000
# HTML страница ✅

# Проверка токенов
curl http://localhost:8000/api/tokens
# Список токенов ✅
```

## 🛠️ Что было исправлено

1. **Восстановлен файл `main.py`** - удален поврежденный код
2. **Созданы скрипты запуска** - `run_backend.sh` и `run_frontend.sh`
3. **Исправлена обработка портов** - автоматическая остановка занятых портов
4. **Добавлены проверки** - виртуальное окружение, зависимости

## 🎯 Готовые команды для копирования

### Запуск backend:
```bash
cd /home/urch/Projects/yandex_downloads && ./run_backend.sh
```

### Запуск frontend:
```bash
cd /home/urch/Projects/yandex_downloads && ./run_frontend.sh
```

### Запуск всего:
```bash
cd /home/urch/Projects/yandex_downloads && ./start.sh
```

## 🎉 Результат

- ✅ Backend работает на http://localhost:8000
- ✅ Frontend работает на http://localhost:3000
- ✅ Все API эндпоинты доступны
- ✅ Токены сохраняются и работают
- ✅ Приложение готово к использованию

**Теперь вы можете снова запускать приложение через Cursor!** 🚀
